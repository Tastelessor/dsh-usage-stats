/** Placeholder section body; replaced by the full page in Task 8. */
export interface UsageStatsSectionProps {
  placeholder: string
}

export function UsageStatsSection({ placeholder }: UsageStatsSectionProps): JSX.Element {
  return <div>{placeholder}</div>
}
